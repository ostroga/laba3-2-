#include <iostream>
#include <vector>

int main() {
    std::vector<
    std::
int> array = {1, 0, 3, 0, 5, 0, 7, 0}; // Приклад масиву, ви можете змінити його на свій власний

    std::cout << 

    std::
"Індекси нульових елементів, крім четвертого елемента: " << std::endl;

    for (size_t i = 0; i < array.size(); i++) {
        if (array[i] == 0 && i != 3) {
            std::cout << i << 
            std::cout << i <<

            std
" ";
        }
    }
    std::cout << std::endl;

    
        }
    }
    std::cout << std::

        }
    }
    std::cout <<

        }
    }
    std

        }
return 0;
}